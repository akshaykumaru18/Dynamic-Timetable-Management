<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akshay.timetable888">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher2_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LecturerActivity" />
        <activity android:name=".Authentication">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".WeekActivity" />
        <activity android:name=".DaySchedule" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SubjectActivity" />
        <activity android:name=".FacultyActivity" />
        <activity android:name=".FacultyDetails" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SelectSemActivity" />
        <activity android:name=".SubjectSelectSem" />
        <activity android:name=".SubjectDetailEditActivity" />
        <activity android:name=".FacultySchedule" />
        <activity android:name=".StudentLayout" />

    </application>

</manifest>